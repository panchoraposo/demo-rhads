# ${{values.component_id}}

Node.js agentic HTTP service. In Dev Spaces run **Red Hat Dependency Analytics** on `package.json`, change a string, commit and push to start the RHADS pipeline.

```
npm install && npm start
```
