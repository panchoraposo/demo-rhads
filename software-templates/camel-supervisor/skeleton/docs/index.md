# ${{values.component_id}}

${{values.description}}

Supervisor car-return workflow (Miles of Smiles step 04) on **Red Hat build of Apache Camel**. Design routes in **Kaoto** (`integrations/03-workflow.camel.yaml`), run them with **Camel JBang** against **Red Hat MaaS**, then push to the RHADS supply chain.

See the repository README for the Dev Spaces and GitOps flow.
